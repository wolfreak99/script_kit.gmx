///bool_select(bool,true,false);
/***************************************************
  USEAGE:
    Returns a variable based on the bool given.
        (Think of " bool ? string_true : string_false"
        in other programming languages..)
    
  VARIABLES:
    bool - determines what will be returned.
    true - returned if bool is true.
    false - returned if bool is false.
 ***************************************************/
if argument0
    return argument1;
else
    return argument2;
