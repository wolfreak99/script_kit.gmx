///instance_change_destroy(newobject)
/***************************************************
  Useage: Creates a new object at the current objects
  place and then destroys the current object.
  The difference between this and instance_change, is that
  this runs the new objects create stage
 ***************************************************/
instance_create(x, y, argument0)
instance_destroy()
